[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "agentic-safety-eval"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "inspect-ai>=0.3",
    "openai>=1.0",
]

[tool.setuptools.packages.find]
include = ["evals*"]
